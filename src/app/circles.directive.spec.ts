import { CirclesDirective } from './circles.directive';

describe('CirclesDirective', () => {
  it('should create an instance', () => {
    const directive = new CirclesDirective();
    expect(directive).toBeTruthy();
  });
});
