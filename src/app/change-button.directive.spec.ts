import { ChangeButtonDirective } from './change-button.directive';

describe('ChangeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
